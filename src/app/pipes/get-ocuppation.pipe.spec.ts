import { GetOcuppationPipe } from './get-ocuppation.pipe';

describe('GetOcuppationPipe', () => {
  it('create an instance', () => {
    const pipe = new GetOcuppationPipe();
    expect(pipe).toBeTruthy();
  });
});
